  default: [INFO]  systemd: Starting k3s
      default: Job for k3s.service failed because the control process exited with error code.
          default: See "systemctl status k3s.service" and "journalctl -xe" for details.
	  The SSH command responded with a non-zero exit status. Vagrant
	  assumes that this means the command failed. The output for this command
	  should be in the log above. Please read the output to determine what
	  went wrong.
	  evansjr@Johns-Mac-Studio(155)$
	  ~B